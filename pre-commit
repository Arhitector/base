# less path to img without variables
# less fail compilation files
# jade path to img without variables
# jade fail compilation files
# js problem with compilation
PATH="/usr/local/bin:$PATH"
gulp pre-commit